name: Placement_Prediction_Project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: string, default: "train_preprocessing.csv"}
      target_var: {type: string, default: "y"}
    command: "python modelling.py {data_path} {target_var}"